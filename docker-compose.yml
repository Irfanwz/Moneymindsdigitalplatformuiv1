services:
  moneyminds:
    build: .
    container_name: moneyminds-app
    restart: unless-stopped
    env_file: .env.production
    environment:
      - NODE_ENV=production
      - API_PORT=3001
      - CLIENT_ORIGIN=https://moneymindsdigital.site
    ports:
      - "3001:3001"
